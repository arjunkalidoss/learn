global:
  jenkinsAgent: any


ansible:
  stageName: Ansible
  dir: Ansible
  cmd: ansible-playbook -i inventory/${var.env}
  playbookDir: playbook
  playbookList:
    -
    - verify-connection.yml

  PreTask:
  PostTask:
